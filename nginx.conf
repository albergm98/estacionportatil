server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # www → apex (las URL canónicas van sin www)
  if ($host = www.estacionportatil.com) {
    return 301 https://estacionportatil.com$request_uri;
  }

  location / {
    try_files $uri $uri/ =404;
  }

  location ~* \.(css|js|webp|jpg|jpeg|png|svg|woff2|ico|xml|txt)$ {
    expires 7d;
    add_header Cache-Control "public";
  }

  gzip on;
  gzip_types text/css application/javascript application/json image/svg+xml;
}
